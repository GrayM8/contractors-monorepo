{
  "name": "contractors-monorepo",
  "version": "1.0.0",
  "description": "Contractors on the Run! - Colyseus + Phaser monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"pnpm dev:server\" \"pnpm dev:client\"",
    "dev:server": "pnpm -C server dev",
    "dev:client": "pnpm -C client dev",
    "build": "pnpm -C server build && pnpm -C client build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.5.3"
  }
}
